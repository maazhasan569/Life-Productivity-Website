import asyncHandler from "../../utils/asyncHandler"
const createExpense = asyncHandler(async(req,res) => {
    //if there is any loan,goal,
    //if there a budget
    //deduct the amt saved for loan and goals
    //check the spending trends of user->(frontend)
    
})